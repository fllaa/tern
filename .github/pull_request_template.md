## Summary

<!-- What does this PR do, and why? Link the issue if there is one. -->

## Test evidence

<!-- How did you verify it? Command output, screenshots, added tests. -->

## Checklist

- [ ] All commits are signed off (`git commit -s`) — required by the DCO check
- [ ] `cargo fmt` / `cargo clippy -- -D warnings` / `cargo test` pass locally
- [ ] No `tauri` dependency introduced into a `core-*` crate
- [ ] ADR added/updated if this changes an architectural decision
